let Storage,gql,path;_6e6‍.x([["Query",()=>Query],["Mutation",()=>Mutation]]);_6e6‍.w("@google-cloud/storage",[["Storage",["Storage"],function(v){Storage=v}]]);_6e6‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);_6e6‍.w("path",[["default",["path"],function(v){path=v}]]);



let Data = [];

       const Query = {
  getUser: () => {
    return Data;
  },
};

const removeWhiteSpaces = (name) => {
  let newName = name.replace(/\s+/g, "");

  return newName;
};

       const Mutation = {
  createUser: async (_, { username, image }) => {
    const bucketName = "node-graphql-application"; // our GCS bucket name, should be in kept in our .env file

    const storage = new Storage({
      keyFilename: path.join(__dirname, "../remotify-secret-key.json"),
    });

    const { filename, createReadStream } = await image;

    let sanitizedName = removeWhiteSpaces(filename);
    await new Promise((resolve, reject) => {
      createReadStream().pipe(
        storage
          .bucket(bucketName)
          .file(sanitizedName)
          .createWriteStream()
          .on("finish", () => {
            storage
              .bucket(bucketName)
              .file(sanitizedName)
              .makePublic()
              .then(() => {
                //empties the db-storage-like array :)
                Data = [];
                // pushes a new data into the db-storage-like array :)
                Data.push({
                  id: Math.ceil(Math.random() * 100),
                  username: username,
                  imageuri: `https://storage.googleapis.com/${bucketName}/${sanitizedName}`,
                });

                resolve(Data);
              })
              .catch((e) => {
                reject((e) => console.log(`exec error : ${e}`));
              });
          })
      );
    });
  },

  deleteUser: (_, {}) => {
    Data = [];

    if (Data.length < 1) {
      return true;
    } else {
      return false;
    }
  },
};
